# PyTorch — install the right variant for your hardware:
#   CPU only:  pip install torch torchvision
#   CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   Apple MPS: pip install torch torchvision  (MPS support is built in on macOS)
#
# torchvision>=0.15 is required for torchvision.transforms.v2 and T.CutMix
torch>=2.0.0
torchvision>=0.15.0
transformers==4.40.0
timm>=1.0.0
sentencepiece
huggingface_hub

pandas>=1.5.0
numpy>=1.23.0
Pillow>=9.0.0
matplotlib>=3.6.0

# Needed to run the .ipynb notebook
jupyter>=1.0.0
